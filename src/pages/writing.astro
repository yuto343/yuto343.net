---
import BaseLayout from "../layouts/base-layout.astro";
import Timeline from "../components/index/timeline.astro";
import { getCollection } from "astro:content";
const eventCollection = await getCollection("writing");

const sortedEvents = eventCollection.sort(
  ({ data: { date: a } }, { data: { date: b } }) => b.getTime() - a.getTime()
);
---

<BaseLayout>
  <Timeline events={sortedEvents} type='writing' />
</BaseLayout>
